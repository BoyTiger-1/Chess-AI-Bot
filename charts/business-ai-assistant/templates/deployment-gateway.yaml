{{- if .Values.services.gateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "baa.serviceName" (dict "root" . "component" "gateway") }}
  labels:
    {{- include "baa.componentLabels" (dict "component" "gateway" "root" .) | nindent 4 }}
spec:
  replicas: {{ .Values.services.gateway.replicaCount }}
  selector:
    matchLabels:
      {{- include "baa.componentLabels" (dict "component" "gateway" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "baa.componentLabels" (dict "component" "gateway" "root" .) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "baa.fullname" . }}
      containers:
        - name: gateway
          image: "{{ .Values.services.gateway.image.repository }}:{{ .Values.services.gateway.image.tag }}"
          imagePullPolicy: {{ .Values.services.gateway.image.pullPolicy }}
          args:
            - uvicorn
            - ai_business_assistant.gateway.main:app
            - --host
            - 0.0.0.0
            - --port
            - "{{ .Values.services.gateway.service.port }}"
          ports:
            - name: http
              containerPort: {{ .Values.services.gateway.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "baa.fullname" . }}
            - secretRef:
                name: {{ include "baa.fullname" . }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 2
          resources:
            {{- toYaml .Values.services.gateway.resources | nindent 12 }}
        {{- if .Values.observability.sidecar.enabled }}
        - name: observability-sidecar
          image: {{ .Values.observability.sidecar.image | quote }}
          resources:
            {{- toYaml .Values.observability.sidecar.resources | nindent 12 }}
        {{- end }}
{{- end }}
