{{- if .Values.services.auth.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "baa.serviceName" (dict "root" . "component" "auth") }}
  labels:
    {{- include "baa.componentLabels" (dict "component" "auth" "root" .) | nindent 4 }}
spec:
  replicas: {{ .Values.services.auth.replicaCount }}
  selector:
    matchLabels:
      {{- include "baa.componentLabels" (dict "component" "auth" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "baa.componentLabels" (dict "component" "auth" "root" .) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "baa.fullname" . }}
      initContainers:
        - name: db-init
          image: "{{ .Values.services.auth.image.repository }}:{{ .Values.services.auth.image.tag }}"
          imagePullPolicy: {{ .Values.services.auth.image.pullPolicy }}
          command: ["python", "-c", "import asyncio; from ai_business_assistant.shared.db.init_db import init_db; asyncio.run(init_db())"]
          envFrom:
            - configMapRef:
                name: {{ include "baa.fullname" . }}
            - secretRef:
                name: {{ include "baa.fullname" . }}
      containers:
        - name: auth
          image: "{{ .Values.services.auth.image.repository }}:{{ .Values.services.auth.image.tag }}"
          imagePullPolicy: {{ .Values.services.auth.image.pullPolicy }}
          args:
            - uvicorn
            - ai_business_assistant.auth_service.main:app
            - --host
            - 0.0.0.0
            - --port
            - "{{ .Values.services.auth.service.port }}"
          ports:
            - name: http
              containerPort: {{ .Values.services.auth.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "baa.fullname" . }}
            - secretRef:
                name: {{ include "baa.fullname" . }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 2
          resources:
            {{- toYaml .Values.services.auth.resources | nindent 12 }}
        {{- if .Values.observability.sidecar.enabled }}
        - name: observability-sidecar
          image: {{ .Values.observability.sidecar.image | quote }}
          resources:
            {{- toYaml .Values.observability.sidecar.resources | nindent 12 }}
        {{- end }}
{{- end }}
