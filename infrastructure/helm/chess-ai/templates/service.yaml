apiVersion: v1
kind: Service
metadata:
  name: {{ include "chess-ai.fullname" . }}
  labels:
    {{- include "chess-ai.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chess-ai.selectorLabels" . | nindent 4 }}
