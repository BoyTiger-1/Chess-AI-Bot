# AWS EKS example

This example provisions:

- A VPC with public/private subnets and NAT
- An EKS cluster with a default managed node group

## Usage

```bash
cd infra/terraform/examples/aws-eks
terraform init
terraform apply
```
