# Core runtime + test dependencies for Business AI Assistant.
#
# Heavy/optional ML dependencies (transformers/torch/tensorflow/prophet/etc.) have been
# intentionally excluded from this base set to keep installs fast and reliable.
# Modules that can leverage those libraries should import them lazily and degrade
# gracefully when they're not available.
#
# Optional ML/AI Dependencies (install as needed):
#   transformers>=4.0.0   # For NLP tasks, embeddings, RAG
#   torch>=2.0.0           # PyTorch for ML models
#   tensorflow>=2.13.0     # Alternative ML framework
#   prophet>=1.1.0         # Time series forecasting
#   sentence-transformers  # Semantic search embeddings
#   openai>=1.0.0          # OpenAI API client
#   anthropic>=0.18.0      # Anthropic API client

# API / microservices (REQUIRED)
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
slowapi==0.1.9
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Data / persistence (REQUIRED)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
redis==5.2.1
celery==5.4.0
kombu>=5.3.0  # Required for Celery RabbitMQ support

# Security (REQUIRED)
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
python-multipart>=0.0.6,<0.0.7

# Observability (REQUIRED)
prometheus-client==0.21.1
structlog==23.2.0

# Analytics used by unit tests and data processing (REQUIRED)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
textblob>=0.17.1

# Warehouse / ETL (REQUIRED)
duckdb>=1.0.0
requests
tenacity

# Testing (REQUIRED)
pytest>=7.4.0
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
